import { _decorator, Component, Node } from 'cc';

export enum Bundle{
    Gui = "GUI",
    Config = "CONFIG"
}

export enum Config{
    UIConfig = "ui_config"
}

export enum Gui{
    MainUI = "MainUI",
    PopupUI = "PopupUI"
}

export enum EventType{
    UI    
}

export enum UIType{
    MainUI,
    PopupUI
}


